apiVersion: v1
kind: ServiceAccount
metadata:
  name: worker-sa # 규약에 명시된 이름 강제
  namespace: hailcast
  annotations:
   # 인프라팀이 추후 생성할 IAM Role의 ARN이 들어갈 자리입니다.
    eks.amazonaws.com/role-arn: "arn:aws:iam::123456789012:role/hailcast-worker-irsa"